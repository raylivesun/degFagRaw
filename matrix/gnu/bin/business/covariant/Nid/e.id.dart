import 'dart:convert';

var e = identical(a, b);

mixin b {
  late String occurrences = base64UrlEncode(bytes!);

  List<int>? get bytes => null;
}

mixin a {
  late String occurrences = base64UrlEncode(bytes!);

  List<int>? get bytes => null;
}

main() {
  return;
}

